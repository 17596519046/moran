<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.LaboratoryMapper">

    <insert id="insertLaboratory">
        INSERT INTO laboratory (name, buildingNo, createTime, isAppointment,updateTime)
        VALUES
        (#{name} ,#{buildingNo} ,#{createTime} ,#{isAppointment},#{updateTime}  )
    </insert>

    <update id="updateLaboratory">
        update laboratory set name = #{name} ,buildingNo = #{buildingNo} ,isAppointment = #{isAppointment},updateTime = #{updateTime}
         where id = #{id}
    </update>

    <select id="selectList" resultType="com.library.pojo.Laboratory">
        select * from laboratory
    </select>

    <select id="selectLaboratory" resultType="com.library.pojo.Laboratory">
        SELECT * from laboratory where id = #{id}
    </select>

    <delete id="deleteLaboratory">
        delete from laboratory where id = #{id}
    </delete>
</mapper>
